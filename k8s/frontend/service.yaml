apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: stuffie-collection
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
  # ClusterIP（デフォルト）: 外部公開はIngressが担当する。
