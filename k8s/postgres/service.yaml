apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: stuffie-collection
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP（デフォルト）: クラスタ内からのみアクセス可能。外部には公開しない。
