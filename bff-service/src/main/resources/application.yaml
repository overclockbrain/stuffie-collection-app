spring:
  security:
    jwt:
      secret: ${JWT_SECRET:your-secret-key-change-in-production}
      expiration: 3600

stuffed-animal-service:
  url: ${STUFFED_ANIMAL_SERVICE_URL:http://stuffed-animal-service:8081}

server:
  port: 8080
